import { beforeAll, afterAll, afterEach } from 'vitest';
// Add any global test setup here
// e.g., database connection, cleanup, etc.

beforeAll(async () => {
  // Connect to test database
});

afterEach(async () => {
  // Clean up after each test
});

afterAll(async () => {
  // Close database connections
});